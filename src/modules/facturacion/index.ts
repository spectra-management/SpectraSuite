/**
 * Facturación (Invoicing) module — placeholder.
 *
 * Not yet implemented. The route ('/facturacion') renders the shared <ModuleShell> "Coming
 * Soon" surface (see src/App.tsx). When this module is built, expose its pages and
 * public API from this barrel, mirroring src/modules/nomina/index.ts.
 */
export {}
